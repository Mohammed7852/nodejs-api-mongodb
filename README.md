#node.js first project 

##build & development 


`$ npm i ts-node  --save`

`$ npm i @types/express @types/mongoose @types/node ts-node-dev typescript --save-dev `

`$ tsc  //this command to build and check no errors`

`$ npm run local  //this command to serve project locally`